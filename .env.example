# Production server port. Docker maps the same port by default.
PORT=3001
# Optional: allowed web origin(s) in production, comma-separated when needed.
# Example: https://game.example.com,https://admin.game.example.com
CLIENT_ORIGIN=
# Optional absolute path for durable game records. Defaults to ./storage/game-history.json.
GAME_HISTORY_PATH=
